name: object_detector
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  simple_logger: ^1.7.0
  flutter_riverpod: ^0.12.4
  flutter_native_splash: ^0.3.0
  tflite_flutter: ^0.5.0
  tflite_flutter_helper: ^0.1.2
  camera: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/tflite/

flutter_native_splash:
  color: '#2196f3'
  image: 'assets/images/syougatsu2_mochi.png'
